# Resultados de scripts

Salidas generadas al ejecutar los scripts de [`../scripts/`](../scripts/). La carpeta refleja la misma organización:

| Carpeta | Contenido |
|---------|-----------|
| `construir_red/` | Export PF (`lineas_parametros.csv`), log de sincronización de nodos |
| `validar_red/` | Comparaciones OR vs PF, validación del modelo |
| `flujo_carga/` | 6 escenarios LF (CSV, reporte, figuras) |

Los archivos aquí se regeneran al correr cada script; no editar manualmente salvo notas puntuales.
